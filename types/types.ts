export type ModalElements = {
    modal: HTMLElement | null;
    openModalBtn: HTMLElement | null;
    closeBtn: HTMLElement | null;
};

export type HeaderElement = HTMLElement | null;

export type FetchButton = HTMLElement | null;